#!/usr/bin/env bash
set -eo pipefail

plan_path=$2

echo "Extension detect, with plan path ${plan_path}"

if [ -f pom.xml ]; then
  echo "Extension adding to build plan"
  cat >> "${plan_path}" <<EOF
[[provides]]
name = "maven"

[[provides]]
name = "java"
EOF

fi

# Always detect
exit 0
